teleop_iphone:
  ros__parameters:
    # iPhone TF frames
    iphone_source_frame: "iphone_odom"
    iphone_target_frame: "iphone_ros"
    # Arm base frame (must match arx5_ros2's "base_frame" param; used as frame_id on outgoing EE commands)
    arm_base_frame: "base_link"

    eef_cmd_topic: "/arx5_controller/eef_cmd"
    gripper_cmd_topic: "/arx5_controller/gripper_cmd"
    eef_state_topic: "/arx5_controller/eef_state"
    reset_service_name: "/arx5_controller/reset_to_home"

    # Touch radius threshold to detect pressing
    touch_threshold: 0.3
    touch_timeout: 0.05

    # Scale factor for position mapping (iphone movement -> EE movement)
    position_scale: 1.2
    rotation_scale: 0.6

    # Gripper positions
    gripper_open: 0.04 # 0.07
    gripper_close: 0.005

    control_rate: 60 # hz
